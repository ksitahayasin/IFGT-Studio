"use client";
import { motion } from "framer-motion";
import { Container } from "@/components/ui/Container";
export function PageHero({ eyebrow, title, copy }: { eyebrow: string; title: string; copy: string }) { return <section className="relative overflow-hidden border-b border-white/[.08] pt-36 sm:pt-44"><div className="absolute inset-0 grid-bg opacity-20"/><div className="absolute right-[10%] top-0 h-72 w-72 rounded-full bg-blue-600/20 blur-[100px]"/><Container className="relative pb-16 sm:pb-20"><motion.p initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} className="text-xs font-bold uppercase tracking-[.25em] text-blue-400">{eyebrow}</motion.p><motion.h1 initial={{ opacity: 0, y: 30 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .6 }} className="mt-5 max-w-4xl font-display text-5xl font-medium tracking-[-.06em] sm:text-7xl">{title}</motion.h1><p className="mt-6 max-w-xl text-base leading-7 text-zinc-400 sm:text-lg">{copy}</p></Container></section>; }
