# IFGT Studio styles

Global design tokens and base styling are intentionally kept in `app/globals.css` so Next.js can load them at the application root. This directory is reserved for future component-level style primitives that do not belong in Tailwind utilities.
